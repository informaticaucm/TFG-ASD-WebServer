<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Mis faltas no justificadas - ASD</title>
                <!--Cabecera comun de head-->
        <%- include('Plantillas/plantilla-head-html.ejs') %>
    </head>
    <body class="sb-nav-fixed">
        <!--barra de navegacion -->
        <%- include('Plantillas/plantilla-nav-var.ejs') %>
        <div id="layoutSidenav">
            <!-- Plantilla de sidevar-->
            <%- include('Plantillas/plantilla-layout-sidevar.ejs')%>
            <div id="layoutSidenav_content">
                <main>
                    <div class="container-fluid px-5">
                        <ol class="breadcrumb mb-4 py-4">
                            <li class="breadcrumb-item"><a href="/">Página principal</a></li>
                            <li class="breadcrumb-item active">Mis faltas no justificadas</li>
                        </ol>
                        <h1 class="text-title">Mis faltas</h1>
                        <div class="row justify-content-center" id="feature-div">
                            <div class="mt-5 col-11 col-sm-10 col-md-8 col-lg-6 my-auto">
                                <!-- separar de arriba, en xs 11 columnas, en s 10 columnas, etc. -->
                                <% clases.forEach(function(clase) { %>
                                    <a href="/registro-motivo-falta/?fecha=<%=clase.fechayhora %>&clase=<%=clase.clase%>&i=<%=clase.pos%>">
                                        <div class="card mt-3">
                                            <div class="card-body">
                                                <p class="text-center"><%=clase.fechayhora %></p>
                                                <% clase.clase.forEach(function(cl) { %>
                                                    <p class="text-center"><%= cl %></p>
                                                <% }); %>
                                            </div>
                                        </div>
                                    </a>
                                <% }); %>
                            </div>
                        </div>
                    </div>
                </main>
            </div>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
        <script src="../assets/js/scripts.min.js"></script>
    </body>
</html>