<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Aulas - ASD</title>
                <!--Cabecera comun de head-->
        <%- include('Plantillas/plantilla-head-html.ejs') %>
    </head>
    <body class="sb-nav-fixed">
        <!--barra de navegacion -->
        <%- include('Plantillas/plantilla-nav-var.ejs') %>
        <div id="layoutSidenav">
            <!-- Plantilla de sidevar-->
            <%- include(Plantillas/plantilla-layout.sidevar.ejs)%>
            <div id="layoutSidenav_content">
                <main>
                    <!-- espacio necesario para que el navbar no se coma parte de los datos -->
                    <div class="container-fluid px-4"> 
                        <ol class="breadcrumb mb-4 py-4">
                            <li class="breadcrumb-item"><a href="/">Página principal</a></li>
                            <li class="breadcrumb-item active">Aulas</li>
                        </ol>
                        <div class="row justify-content-center" id="feature-div">
                        <!-- separar de arriba, en xs 11 columnas, en s 10 columnas, etc. -->
                            <div class="col-11 col-sm-10 col-md-8 col-lg-6">
                                <form method="post">
                                    <% espacios.forEach(function(edificio) { %>
                                        <div class="py-3">
                                        <h3>Edificio <%= edificio.edificio %></h3>
                                        <% edificio.espacios.forEach(function(espacio) { %>
                                        <div class="row justify-content-center mt-4" id="formulario-feature">
                                            <button class="btn btn-primary btn-feature" name="espacio" type="submit" value="<%= espacio.id %>"><%= espacio.nombre %></button>
                                        </div>
                                        <% }); %>
                                        </div>
                                    <% }); %>
                                    <% if (!all) { %>
                                        <div class="row justify-content-center mt-4 mb-4" id="formulario-feature">
                                            <button class="btn btn-primary btn-feature" name="espacio" type="submit" value="Otro">Otro</button>
                                        </div>
                                    <% } %>
                                </form>
                            </div>
                        </div>
                    </div>
                </main>
            </div>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
        <script src="../assets/js/scripts.min.js"></script>
    </body>
</html>